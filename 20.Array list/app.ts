// Array of countries
let countryList: string[] = ['Pakistan', 'Canada', 'Russia', 'China', 'Japan'];

console.log('List of Countries:');
for (let i = 0; i < countryList.length; i++) {
    console.log(`${i + 1}. ${countryList[i]}`);
}
